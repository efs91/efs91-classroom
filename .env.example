# EFS91 Classroom - Configuration
# Copiez ce fichier en .env et modifiez les valeurs selon votre setup

# Mode de fonctionnement : "localhost" ou "cloudflare"
MODE=localhost

# === CONFIGURATION MODE LOCALHOST ===
# Pour usage local uniquement (pas de tunnel requis)
# Accès via http://localhost:3000/prof.html
# Sessions : https://localhost:5801, 5802, etc.

# === CONFIGURATION MODE CLOUDFLARE ===
# Décommentez et configurez si MODE=cloudflare

# Nom de votre tunnel Cloudflare
# TUNNEL_NAME=votre-tunnel

# Votre domaine personnalisé (sans https://)
# DOMAIN=votre-domaine.com

# Port de l'orchestrateur (optionnel, défaut: 3000)
# PORT=3000